# Datasets (Phase 4 — training-data factory)

Bulk data is gitignored; only small tracked samples live here. Phase 4 produces
(intent → IR/binary) pairs *with reasoning* by compiling (problem, source-solution, tests)
tuples at multiple optimization levels, plus harvested Phase 3 repair trajectories.
See `docs/this-plan.md` §Phase 4. Not yet built.
